# Datasource properties
ds.driverClassName=${ds.driverClassName}
ds.url=${ds.url}
ds.username=${ds.username}
ds.password=${ds.password}

# Test properties
version=${project.version}
artifactId=${project.artifactId}
logpath=${app.logpath}
jdk.version=${jdk.version}